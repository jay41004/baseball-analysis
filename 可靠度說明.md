# 可靠度說明：定時更新會不會又壞？

## 直接講結論

**免費 GitHub Pages + Render 無法保證「永遠正確、永遠不壞」。**  
能大幅降低「今天修好明天又掛」的機會，但不能 100%。

| 方案 | 穩定度 | 費用 | 建議 |
|------|--------|------|------|
| **GitHub Pages（定時 Actions）** | 中高 | 免費 | **手機主站**，每 2 小時更新 |
| Render 免費版 | 低～中 | 免費 | 只當備援；會睡、記憶體小易 502 |
| **本機常駐 / 便宜 VPS** | 高 | 電費或約 USD $4～6／月 | 要「自己的站、少壞」選這個 |

壞掉常見原因（不是你操作錯）：對方官網改版、Actions 排隊取消、Render 睡死／記憶體爆、單場資料源暫時沒先發。

---

## 現在已加強的（GitHub Pages）

1. **每 2 小時**自動完整更新（不再每次改程式就重跑大更新，避免互相取消）
2. 單一球隊失敗會繼續，不會整聯盟全掛
3. 新資料比舊的還少時，**保留上一版**再上線（避免「更新失敗→網站變空」）
4. Render 免費版改為**鏡像 Pages**，兩邊同一份數據，不再各抓各的

**手機請固定用：**  
https://jay41004.github.io/baseball-analysis/

手動立刻更新：GitHub → Actions → `Refresh data and deploy GitHub Pages` → Run workflow

---

## 若還是不放心：自己架常駐站

### 方案 A — 你家電腦（免費）

1. 雙擊 **`本機常駐更新.bat`**（電腦要開著）
2. 區網用 `http://你的電腦IP:8000`
3. 要外網也能開：用 [Cloudflare Tunnel](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/) 或路由器轉發 8000

這是完整模式（`CLOUD_LITE=0`），行為接近本機 `run.bat`，不依賴 Render。

### 方案 B — 便宜 VPS（推薦長期）

任一雲：DigitalOcean / Linode / Vultr / Oracle Cloud 免費 ARM 等。

```bash
git clone https://github.com/jay41004/baseball-analysis.git
cd baseball-analysis
docker compose up -d --build
```

瀏覽器開：`http://伺服器IP:8000`  
要 HTTPS 再前面加 Caddy / Nginx 即可。

VPS 上是**你的機器、你的排程**，不會被 Render 免費額度睡掉。

---

## 你該怎麼選

- 只想手機看、可接受最多約 2 小時舊一點 → **只用 GitHub Pages**
- 想盡量少壞、願意電腦開著或付一點錢 → **本機常駐或 VPS**
- 不要再同時把 Render 當「正確來源」和 Pages 比對，會一直覺得兩邊打架
